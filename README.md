# Actividad M2A3: HTML semántico

Se cambió la estructura basada en `div` por etiquetas semánticas como `header`, `nav`, `main`, `article`, `aside`, `section` y `footer`.
También se usaron `time`, `address`, `abbr` y `blockquote` para dar significado al contenido.
